{
    "title": "options",
    "description": "Configuration options of navigaiton bar",
    "examples": [
        {
            "description": "A simple light colored navigation bar.",
            "colorScheme": "navbar-light",
            "backgroundColorClass": "bg-light"
        },
        {
            "colorScheme": "navbar-light",
            "backgroundColorClass": "bg-light",
            "backgroundColor": "red"
        }
    ],
    "properties": {
        "colorScheme": {
            "type": "string",
            "title": "The Color Scheme",
            "description": "Color scheme can be light or dark",
            "default": "",
            "enum": ["navbar-light", "navbar-dark"]
        },
        "backgroundColorClass": {
            "type": "string",
            "title": "Background Color Class",
            "description": "Bootstrap class for background color. Refer: https://getbootstrap.com/docs/5.0/utilities/background/ ",
            "default": "",
            "examples": [
                "bg-light", "", "bg-dark"
            ]
        },
        "backgroundColor": {
            "type": "string",
            "title": "Background Color",
            "description": "Any color other than standard bootstrap classes like primary, secondary, dark, etc. Could be any of standard color names: https://www.w3schools.com/colors/colors_hex.asp. Or these could be any hex values like #00bfff. If backgroundColor is specified, backgroundColorClass gets ignored",
            "default": "",
            "examples": [
                "#e3f2fd", "red", "black"
            ]
        }
    }
}